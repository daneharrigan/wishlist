<% collection.in_groups_of(4, false).each do |cards| %>
	<div class="row">
		<% cards.each do |card| %>
			<%= render partial: "layouts/card", collection: cards, as: :item %>
			<%# Uncomment this if you want it to work properly %>
			<%# render partial: "layouts/card", locals: { item: card } %>
		<% end %>
	</div>
<% end %>
